# ─── Build / runtime artifacts ─────────────────────────────────────────────

# ICC profiles are NOT redistributable in general (Adobe / ECI / Mimaki
# licenses vary). Bring your own; this tree never carries them.
icc/*.icc
icc/*.icm
icc/*.ICC
icc/*.ICM
# icc/luts/

# Generated lookup tables (large; rebuilt by scripts/gen_luts.py).
# data/luts/*.lut
# data/luts/index.json

# Generated curated libraries (rebuilt by scripts/gen_libraries.py).
# data/libraries/*.json
# data/libraries/*.ase
# data/libraries/*.gpl

# Local HTTP server launchers (developer convenience; the README
# documents the underlying `python -m http.server 8765` command).
run.bat
run.sh

# ─── Python ────────────────────────────────────────────────────────────────

__pycache__/
*.pyc
*.pyo
.venv/
venv/
.python-version

# ─── Test / scratch artifacts (left over from dev sessions) ────────────────

*.png
*.tmp
manifest.txt
manifest.csv
phase*.png
cmyk-current-state.png

# Allow brand assets (logos + certification badges) committed under
# app/img/ even though the *.png blanket above blocks PNGs by default.
!app/img/*.png

# ─── Tool dirs ─────────────────────────────────────────────────────────────

.claude/
.agents/
.playwright-mcp/
.idea/
.vscode/
swatches/
skills-lock.json
CLAUDE.md

# ─── OS noise ──────────────────────────────────────────────────────────────

.DS_Store
Thumbs.db
desktop.ini
